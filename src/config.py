#!/bin/python

config = {
        # MySQL information
        # Host is usually localhost, if database is running on another machine
        # it should be change to address of that machine.
        'mysql_host':   'localhost',
        # User name
        'mysql_user':   'cast',
        # Password
        'mysql_pass':   'castpass',
        # Name of the DATABASE (not a table)
        'mysql_table':  'loggersdb',

        # Socket parameters
        'host': 'localhost',
        'port': 5000
}
