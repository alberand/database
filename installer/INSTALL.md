Installation guide:

```sh
# Edit config.cfg

# Run installatin script
sudo ./install.sh
# Change user. Name placed in configuration file.
su database 
# Go to home directory
cd
# Go to server directory and then to source files directory
cd server_01/src
# Run server
python3 ./main.py

# Now you should see some logging output from the running server.
```
